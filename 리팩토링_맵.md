# 지도 화면 리팩토링 기획서

## 1. 문제 정의

현재 지도 화면은 한 화면 안에 다음 기능이 동시에 노출된다.

- 지도 위 리뷰된 카페 마커
- 상단 카페 검색창
- 전체/로컬/프랜차이즈/추천순/평점순/가까운 순 등 필터칩
- 하단 드래그 바텀시트 카페 리스트
- 선택된 카페 또는 검색된 장소 카드

사용자 관점에서 이 구조는 “지도에서 주변 카페를 찾는 화면”인지 “랭킹/필터 리스트를 보는 화면”인지 목적이 섞인다. 특히 모바일에서는 지도 영역이 작아지고, 상단 필터칩과 하단 리스트가 동시에 지도 공간을 차지해 탐색 흐름이 복잡해진다.

## 2. 기획자 검토

### 핵심 사용자 의도

지도 화면의 1차 의도는 “내 주변에서 카페 위치를 보고 선택하는 것”이다.

### 제안 방향

- 지도 화면에서는 랭킹/순위 탐색을 제거한다.
- 리뷰가 이미 있는 카페는 지도 위에 계속 보여준다.
- 로컬/프랜차이즈/추천순/평점순 같은 필터칩은 제거한다.
- “새로운 카페” 진입점을 추가한다.
- 사용자가 “새로운 카페”를 누르면 현재 위치 기준의 주변 카페 검색 결과를 지도에 표시한다.
- 새 카페 마커를 누르면 네이버 지도 스타일의 간단한 장소 카드가 바텀에 뜬다.

### 비판적 판단

요청의 “현재의 핀을 모두 제거한다. (추천순, 로컬 등등)”는 실제 지도 핀보다 상단 필터칩 제거에 가깝다. 리뷰된 카페 마커까지 제거하면 “리뷰가 되어진 녀석들은 그대로 보여준다”와 충돌한다. 따라서 본 기획에서는 다음처럼 해석한다.

- 제거 대상: 상단 필터칩과 바텀시트 랭킹 리스트
- 유지 대상: 리뷰된 카페 지도 마커
- 신규 추가 대상: “새로운 카페” 모드와 검색된 주변 카페 마커

## 3. 디자이너 판단

### 권장 화면 구조

```text
┌─────────────────────────┐
│ 지도 전체 화면          │
│                         │
│  [리뷰된 카페 마커]      │
│  [새 카페 마커]          │
│                         │
│  상단: [새로운 카페] CTA │
│                         │
│  하단: 선택 장소 카드    │
└─────────────────────────┘
```

### 제거할 UI

- 하단 카페 리스트 바텀시트
- 전체/로컬/프랜차이즈 필터칩
- 추천순/평점 높은 순/가까운 순/리뷰 많은 순/커피 맛 좋은/작업하기 좋은/조용한/디저트 좋은 필터칩
- “추천순은 거리, 리뷰 수 신뢰도, 평점을 함께 반영해요.” 안내 문구

### 유지할 UI

- 지도
- 상단 카페 검색창 또는 “새로운 카페” CTA 중 하나
- 리뷰된 카페 마커
- 선택된 리뷰 카페 요약 카드
- 선택된 새 카페 요약 카드와 리뷰 작성 CTA

### 추천 UX

- 지도 상단에는 검색창을 크게 두지 말고, `새로운 카페 찾기` 버튼을 한 개만 둔다.
- 버튼을 누르면 현재 위치 기반으로 `카페` 또는 `근처 카페` 검색을 실행한다.
- 검색 중에는 버튼 아래 또는 지도 중앙에 작은 로딩 상태를 보여준다.
- 새 카페 마커는 기본 핀 또는 얇은 outline marker로 표시해 리뷰된 카페 마커와 구분한다.
- 리뷰된 카페 마커는 커피 아이콘을 유지한다.
- 새 카페 선택 카드의 CTA는 `리뷰 쓰기` 하나만 둔다.

## 4. 백엔드 개발자 판단

### 현재 사용 가능한 API

- `GET /api/cafemap/stores`: 리뷰가 쌓인 카페 목록
- `GET /api/cafemap/places/search?query=...`: 네이버 지역 검색 기반 장소 검색

### 필요한 변경

MVP에서는 백엔드 변경 없이 기존 `places/search`를 재사용할 수 있다.

다만 “현재 위치 주변 반경” 요구를 정확히 만족하려면 백엔드 계약 개선이 필요하다.

```text
GET /api/cafemap/places/search?query=카페&lat=37.56&lng=126.97&radius=1000&display=20
```

현재 네이버 지역 검색 API가 좌표 기반 반경 검색을 직접 지원하지 않는다면, 백엔드는 다음 중 하나를 선택해야 한다.

- `query=카페`와 현재 행정동/주소를 조합해 검색한다.
- 검색 결과를 받은 뒤 프론트/백엔드에서 좌표 거리 계산으로 필터링한다.
- 정확한 반경 검색이 필요하면 네이버 지도/장소 API 중 좌표 기반 검색 가능 여부를 별도 확인한다.

### 백엔드 수용 기준

- 리뷰된 카페 API는 기존처럼 리뷰가 있는 카페만 반환한다.
- 새 카페 검색 API는 DB에 없는 장소도 반환한다.
- 새 카페 검색 결과에는 최소 `name`, `roadAddress`, `address`, `placeId`, `mapx`, `mapy`, `category`가 포함된다.
- 좌표 기반 반경 검색을 도입한다면 프론트/백엔드 계약 문서에 `lat`, `lng`, `radius`, `display`를 명시한다.

## 5. 프론트엔드 개발자 판단

### 현재 코드 위치

- 지도 화면: `front/lib/presentation/pages/map_home_page.dart`
- 지도 추상화: `front/lib/presentation/widgets/naver_map_view.dart`
- 웹 지도 구현: `front/lib/presentation/widgets/naver_map_view_web.dart`
- 모바일 지도 구현: `front/lib/presentation/widgets/naver_map_view_mobile.dart`
- 리뷰된 카페 provider: `nearbyStoresProvider`
- 장소 검색 provider: `placeSearchRepositoryProvider`

### 리팩토링 방향

`MapHomePage`에서 상태를 세 가지로 분리한다.

- 리뷰된 카페 상태: `List<StoreSummary>`
- 새 카페 검색 상태: `List<PlaceSearchResult>`
- 선택 상태: `selectedReviewedStore` 또는 `selectedNewPlace`

현재 `_selectedStore`, `_selectedPlace`, `_results`, `_selectedSortType`, `_selectedSegment`, `_sheetExtent`가 한 클래스에 같이 섞여 있다. 리팩토링 후에는 `_selectedSortType`, `_selectedSegment`, `_sheetExtent`, `DraggableScrollableSheet`를 제거할 수 있다.

### 예상 UI 상태

```text
기본 상태:
- 리뷰된 카페 마커만 표시
- 상단에 "새로운 카페 찾기" 버튼
- 선택 카드 없음

새로운 카페 탐색 상태:
- 리뷰된 카페 마커 유지
- 주변 새 카페 마커 추가 표시
- 검색 결과 리스트 바텀시트는 표시하지 않음

리뷰된 카페 선택:
- 지도 카메라 이동
- 바텀에 카페명, 별점, 리뷰 수, 상세 이동 카드 표시

새 카페 선택:
- 지도 카메라 이동
- 바텀에 장소명, 주소, 리뷰 쓰기 버튼 표시
```

### 구현 슬라이스

1. 하단 랭킹 리스트 제거
2. 필터칩과 정렬 상태 제거
3. `새로운 카페 찾기` CTA 추가
4. CTA 클릭 시 현재 위치 기반 장소 검색 실행
5. 검색 결과를 새 카페 마커로 지도에 추가
6. 새 카페 마커 탭 시 바텀 장소 카드 표시
7. 리뷰된 카페 마커 탭 시 기존 상세 카드 유지

## 6. 리뷰어 검토

### 주요 리스크

- `places/search`가 실제 반경 검색이 아닐 수 있다.
- 네이버 검색 결과와 DB 리뷰 카페가 중복 표시될 수 있다.
- 웹 지도와 모바일 지도 구현의 마커 갱신 방식이 다를 수 있다.
- 하단 리스트 제거 후 사용자가 “리뷰된 카페 목록을 한눈에 보는 방법”을 잃을 수 있다.
- “새로운 카페”가 버튼인지 지도 핀인지 표현이 불명확하면 다시 혼란이 생길 수 있다.

### 리뷰어 권고

- MVP에서는 “새로운 카페”를 지도 핀보다 상단 CTA 버튼으로 구현한다.
- 새 카페 마커와 리뷰된 카페 마커의 시각 스타일을 반드시 다르게 둔다.
- 리뷰된 카페와 새 카페가 같은 장소로 추정되면 리뷰된 카페를 우선 표시한다.
- 첫 배포에서는 반경 검색을 “주변 검색”이라고 과장하지 말고 `현재 위치 기준으로 카페를 찾아요` 정도로 표현한다.
- 지도 화면에서 리스트를 제거하는 대신, 랭킹/목록 탐색은 기존 카페랭킹 탭으로 역할을 분리한다.

## 7. 최종 제안

이 리팩토링은 진행하는 편이 좋다. 이유는 지도 화면의 역할을 “공간 탐색”으로 좁히고, 랭킹 화면의 역할을 “순위 탐색”으로 분리할 수 있기 때문이다.

단, “새로운 카페”를 지도 위 고정 핀으로 표현하는 것은 추천하지 않는다. 핀은 장소를 의미하므로 사용자가 실제 장소로 오해할 수 있다. 대신 상단 CTA 버튼 또는 플로팅 액션 버튼으로 시작하는 것이 더 명확하다.

최종 MVP 범위는 다음으로 제한한다.

- 카페 리스트 바텀시트 제거
- 필터칩 제거
- 리뷰된 카페 마커 유지
- `새로운 카페 찾기` CTA 추가
- CTA 클릭 시 장소 검색 결과를 지도 마커로 표시
- 새 카페 마커 선택 시 간단한 장소 카드와 `리뷰 쓰기` CTA 표시

## 8. 완료 기준

- 모바일 폭에서 지도 영역이 화면 대부분을 차지한다.
- 지도 화면에 순위/정렬/필터 UI가 보이지 않는다.
- 리뷰된 카페 마커는 기존처럼 표시된다.
- `새로운 카페 찾기`를 누르면 새 카페 마커가 표시된다.
- 새 카페 마커를 누르면 장소명, 주소, 리뷰 쓰기 버튼이 있는 바텀 카드가 표시된다.
- 리뷰된 카페 마커를 누르면 기존 상세 이동 카드가 표시된다.
- `flutter analyze`가 통과한다.
- 가능하면 로컬 Chrome에서 지도 검색/마커 선택 흐름을 수동 확인한다.
