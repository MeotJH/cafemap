# 개인화 리팩토링

## 목적

이 문서는 현재 Cafemap 코드베이스 기준으로 개인화 리팩토링을 바로 구현할 수 있도록 정리한 확정 기획서다.

- 대상: `front/`, `back/`
- 기준: 현재 라우팅, 현재 스토어/랭킹 API 응답 구조
- 목표: 추상 제안이 아니라 FE/BE가 바로 구현 가능한 작업 명세로 고정

## 최종 구조

바텀 탭은 아래 4개로 고정한다.

- `취향`
- `랭킹`
- `맵`
- `내 리뷰`

각 탭 역할은 아래처럼 분리한다.

- `취향`: 사용자가 고른 취향 기준으로 정렬된 개인화 리스트
- `랭킹`: 기존처럼 전체 사용자 기준의 일반 랭킹
- `맵`: 사용자가 고른 취향에 맞는 카페를 강조하는 지도
- `내 리뷰`: 로그인 사용자의 리뷰 관리 화면

중요:

- `취향` 탭은 설정 화면이 아니다.
- `취향` 탭이 개인화의 메인 결과 화면이다.
- `랭킹` 탭은 개인화하지 않고, 비교 기준으로 남겨둔다.

## 제품 변화 요약

사용자는 처음 들어오면 `어떤 카페를 찾고 있나요?` 질문에서 2~3개의 취향을 고르고, 그 선택은 저장된다. 이후 `취향` 탭에서는 그 기준에 맞는 카페 리스트를 바로 보게 되고, 카드마다 왜 추천되는지 이유가 붙는다. `랭킹` 탭은 그대로 전체 랭킹을 보여줘서 개인화 추천과 일반 인기 순위를 비교할 수 있고, `맵` 탭은 같은 취향 기준으로 맞는 카페를 마커와 하단 카드에서 더 강하게 강조한다.

## 첫 방문 UX

### 진입 방식

- 웹 첫 방문 시 초기 진입 라우트는 `/preference`로 둔다.
- `취향` 탭 페이지 진입 직후 `취향` 질문 시트를 1회 노출한다.
- 재방문 시에는 자동으로 다시 띄우지 않는다.
- 저장된 취향이 없을 때만 자동 노출한다.
- `건너뛰기`를 누르면 시트를 닫고 `취향` 탭 기본 리스트를 그대로 보여준다.
- `선택 완료`를 누르면 시트를 닫고 선택한 취향 기준으로 리스트를 즉시 재조회한다.

### 초기 라우팅 규칙

- 앱 시작 시 로컬 저장소를 먼저 읽는다.
- 저장값이 없거나 `hasCompletedInitialSelection == false` 이면 초기 진입 라우트는 `/preference`
- 저장값이 있고 `hasCompletedInitialSelection == true` 이어도 초기 진입 라우트는 `/preference`
- 로그인 여부는 초기 라우팅에 영향을 주지 않는다.
- `내 리뷰` 탭의 로그인 가드는 기존처럼 별도 유지한다.
- 즉, 앱의 기본 홈은 항상 `/preference` 이다.

### 질문 문구

- 제목: `어떤 카페를 찾고 있나요?`
- 설명: `2~3개를 고르면 취향 탭과 지도를 그 기준으로 보여드릴게요.`

### 선택 규칙

- 최소 0개, 최대 3개 선택
- `건너뛰기` 허용
- `선택 완료` 시 로컬 저장

### 저장 규칙

- 1차는 비로그인/로그인 모두 로컬 저장
- 저장 키는 `user_preference_selection_v1`
- 저장 payload는 아래 구조로 고정한다.

```json
{
  "selectedPreferenceIds": ["quiet_place", "work_friendly"],
  "hasCompletedInitialSelection": true,
  "updatedAt": "2026-04-28T12:00:00Z"
}
```

### 상태 전이 규칙

- 첫 방문 + 저장값 없음:
  - `/preference` 진입
  - 질문 시트 자동 오픈
- 첫 방문 + `건너뛰기`:
  - `hasCompletedInitialSelection = true`
  - `selectedPreferenceIds = []`
- 첫 방문 + `선택 완료`:
  - `hasCompletedInitialSelection = true`
  - `selectedPreferenceIds = 선택값`
- 재방문 + 저장값 있음:
  - `/preference` 진입
  - 시트 자동 오픈 없음
  - 저장값으로 즉시 취향 리스트 조회

### 라우팅 예외 규칙

- 사용자가 URL로 직접 `/preference`, `/ranking`, `/map` 에 진입하는 것은 허용한다.
- 첫 방문 미완료 사용자가 `/ranking` 또는 `/map` 으로 직접 진입해도 강제 리다이렉트는 하지 않는다.
- 이 경우 상단에 `취향을 고르면 더 잘 맞는 카페를 볼 수 있어요` 배너 또는 CTA를 노출한다.
- `/preference` 는 언제 진입해도 취향 수정 화면으로 동작한다.

## 1차 취향 카탈로그

현재 코드베이스에서 리스트 응답으로 이미 내려오는 집계 축만 사용해서 1차 취향을 확정한다.

현재 1차에서 사용할 축은 `store aggregate.scores_json`에 이미 누적 저장되고 있는 값들이다.

현재 사용 가능한 원천 축:

- 이미 응답에 있는 값
  - `coffeeQualityScore`
  - `workFriendlyScore`
  - `quietnessScore`
- store aggregate에는 저장되어 있고 1차에 신규 응답 추가할 값
  - `seatComfortScore`
  - `serviceScore`
  - `atmosphereScore`
  - `valueScore`
- 2차 확장용 후보 값
  - `acidityBalanceScore`
  - `bodyScore`

이 중 1차 MVP 취향은 아래 7개로 고정한다.

### `quiet_place`

- 라벨: `조용한 곳`
- 설명: `소음이 적고 차분한 카페`
- 사용 점수:
  - `quietnessScore`

### `work_friendly`

- 라벨: `작업하기 좋은 곳`
- 설명: `오래 앉아 작업하기 편한 카페`
- 사용 점수:
  - `workFriendlyScore`

### `good_coffee`

- 라벨: `커피 맛이 좋은 곳`
- 설명: `기본적인 커피 품질이 좋은 카페`
- 사용 점수:
  - `coffeeQualityScore`

### `good_atmosphere`

- 라벨: `분위기가 좋은 곳`
- 설명: `공간 분위기와 무드가 좋은 카페`
- 사용 점수:
  - `atmosphereScore`

### `good_value`

- 라벨: `가성비가 좋은 곳`
- 설명: `가격 대비 만족도가 높은 카페`
- 사용 점수:
  - `valueScore`

### `kind_service`

- 라벨: `응대가 친절한 곳`
- 설명: `서비스와 응대 만족도가 좋은 카페`
- 사용 점수:
  - `serviceScore`

### `seat_comfortable`

- 라벨: `좌석이 편안한 곳`
- 설명: `오래 앉아 있어도 불편하지 않은 카페`
- 사용 점수:
  - `seatComfortScore`

## 1차 취향 매핑표

1차 취향은 모두 단일 축 1:1 매핑으로 고정한다.

| id | 라벨 | 원천 score field | 이유 라벨 | 1차 포함 |
| --- | --- | --- | --- | --- |
| `quiet_place` | 조용한 곳 | `quietnessScore` | `조용함` | Y |
| `work_friendly` | 작업하기 좋은 곳 | `workFriendlyScore` | `작업 적합성` | Y |
| `good_atmosphere` | 분위기가 좋은 곳 | `atmosphereScore` | `분위기` | Y |
| `good_coffee` | 커피 맛이 좋은 곳 | `coffeeQualityScore` | `커피 맛` | Y |
| `good_value` | 가성비가 좋은 곳 | `valueScore` | `가성비` | Y |
| `kind_service` | 응대가 친절한 곳 | `serviceScore` | `응대` | Y |
| `seat_comfortable` | 좌석이 편안한 곳 | `seatComfortScore` | `좌석 편안함` | Y |

## 원본 키 -> 응답 필드 매핑표

백엔드는 `aggregate.scores_json` 의 원본 키를 아래 응답 필드명으로 변환해서 내려준다.

| scores_json key | response field |
| --- | --- |
| `coffee_quality` | `coffeeQualityScore` |
| `work_friendly` | `workFriendlyScore` |
| `quietness` | `quietnessScore` |
| `seat_comfort` | `seatComfortScore` |
| `service` | `serviceScore` |
| `atmosphere` | `atmosphereScore` |
| `value` | `valueScore` |

## 2차로 미루는 취향

아래 취향은 2차로 미룬다.

- `산미 적은 커피`
- `바디감 있는 커피`
- `라떼가 좋은 곳`
- `디저트가 괜찮은 곳`

2차에서 필요한 추가 응답 축:

- `acidityScore`
- `bodyScore`
- `milkBalanceScore`
- `textureScore`
- `outletAccessScore`
- `wifiQualityScore`
- `dessertScore`

## 탭별 상세 동작

## 취향 탭

이 탭은 개인화 리스트 화면이다.

### 목적

- 선택한 취향 기준으로 정렬된 카페 리스트를 보여준다.
- 사용자가 `이 앱은 내 취향대로 추천해주는구나`를 가장 강하게 느끼는 화면이다.

### 상단 구성

- 현재 취향 요약 바
  - 예: `지금 취향: 조용한 곳 + 작업하기 좋은 곳`
- `취향 수정` 버튼
- `초기화` 버튼

### 수정 UX 규칙

- `취향 수정` 클릭 시 첫 방문과 동일한 취향 선택 시트를 다시 연다.
- 시트에서 선택값을 바꾸고 `선택 완료`를 누르면 즉시 리스트 재조회
- `초기화` 클릭 시 선택값을 빈 배열로 저장하고 기본 리스트 재조회

### 리스트 정렬 규칙

- 취향 선택이 있으면 `personalizedScore DESC`
- 취향 선택이 없으면 `displayScore DESC`

### 카드 표시 규칙

각 카드에 아래를 추가한다.

- `personalizedScore`
- `personalizedReasons`

예시:

- `조용함 4.8 · 작업 적합성 4.6`
- `커피 맛 4.7`

### 비어 있을 때

- 취향 선택이 없으면 빈 화면이 아니라 기본 추천 리스트를 보여준다.
- 대신 상단에 `취향을 고르면 더 잘 맞는 카페를 볼 수 있어요` 문구를 보여준다.

### 로딩/에러 규칙

- 첫 진입 재조회 중:
  - 리스트 스켈레톤 또는 기존 로딩 상태 재사용
- 재조회 실패:
  - `취향 기준 추천을 불러오지 못했어요.`
  - `다시 시도` 버튼 제공

### 1차 확정 취향 목록

- `조용한 곳`
- `작업하기 좋은 곳`
- `분위기가 좋은 곳`
- `커피 맛이 좋은 곳`
- `가성비가 좋은 곳`
- `응대가 친절한 곳`
- `좌석이 편안한 곳`

## 랭킹 탭

이 탭은 기존 전체 랭킹을 유지한다.

### 목적

- 개인화 추천과 별개로, 전체 사용자 기준의 일반 랭킹을 제공한다.
- 사용자가 `내 취향 추천`과 `전체 인기 순위`를 비교할 수 있게 한다.

### 유지 사항

- 기존 정렬 방식 유지
- 기존 랭킹 카드 구조 유지
- 1차에서는 취향 파라미터를 붙이지 않음

### 허용 변경

- 바텀 탭/문구 변경에 따른 최소한의 라우팅 정리
- 필요 시 `전체 랭킹` 문구 명확화

## 맵 탭

이 탭은 개인화 지도다.

### 목적

- 같은 지역 안에서도 사용자의 취향에 맞는 카페를 더 눈에 띄게 보여준다.

### 상단 구성

- 현재 취향 요약 바
- `취향 수정`
- 보기 토글
  - `전체 보기`
  - `추천만 보기`

### 수정 UX 규칙

- 맵에서도 `취향 수정` 클릭 시 동일한 취향 선택 시트 오픈
- 수정 완료 시 현재 뷰포트 기준으로 `/stores` 재조회

### 지도 규칙

- `전체 보기`: 전체 카페 표시, 취향 적합 카페는 추천 마커로 강조
- `추천만 보기`: `isPersonalizedMatch == true` 인 카페만 표시

### 하단 카드

추천 마커를 탭하면 카드에 아래를 노출한다.

- `personalizedScore`
- `personalizedReasons`

예시:

- `조용함 4.8`
- `작업 적합성 4.6`

## 내 리뷰 탭

### 목적

- 기존 `내 활동`을 `내 리뷰` 중심으로 재정의한다.

### 1차 범위

- 명칭 변경
- 로그인 유도 문구 정리

### 2차 후보

- 내 리뷰 기반 취향 프로필
- 내 리뷰 요약

## 개인화 계산 규칙

## 입력

사용자 입력:

- 선택된 취향 ID 목록 0~3개

파싱 규칙:

- query 이름: `preferenceIds`
- 형식: CSV string
- 예: `quiet_place,work_friendly,good_value`
- 중복 id는 제거
- 정의되지 않은 id는 무시
- 최대 3개까지만 반영
- 순서는 보존
- 최종 유효 id가 0개면 `미선택`으로 간주

카페 입력:

- `coffeeQualityScore`
- `workFriendlyScore`
- `quietnessScore`
- `seatComfortScore`
- `serviceScore`
- `atmosphereScore`
- `valueScore`
- `displayScore`
- `reviewCount`

## personalizedScore 계산

1차는 단순 가중 평균으로 간다.

정확한 규칙:

1. 선택된 취향 ID를 score field로 변환
2. 각 score field의 값을 같은 가중치 `1.0`으로 평균
3. 값이 없는 score field는 `0`이 아니라 `미집계`로 취급
4. 선택된 취향 중 실제 응답값이 있는 항목만 평균에 포함
5. 포함 가능한 score가 하나도 없으면 `displayScore` fallback

### 미집계 판정 규칙

- `aggregate.scores_json` 에 해당 원본 키가 없으면 `미집계`
- `aggregate.scores_json` 에 키가 있으면 `0.0` 이어도 실제 값으로 간주
- 즉, 미집계 여부는 숫자값이 아니라 `키 존재 여부`로 판단한다.

백엔드 구현 규칙:

- personalization 계산은 response DTO가 아니라 `scores_json` 원본 dict 기준으로 수행한다.
- 응답 DTO는 하위 호환을 위해 수치 필드를 `0.0` default로 내려줄 수 있다.
- 하지만 내부 계산과 이유 생성은 반드시 원본 dict의 키 존재 여부를 함께 사용한다.

예:

- 선택 취향: `quiet_place`, `work_friendly`
- 카페 점수:
  - `quietnessScore = 4.8`
  - `workFriendlyScore = 4.6`

결과:

- `personalizedScore = (4.8 + 4.6) / 2 = 4.7`

취향이 없을 때:

- `personalizedScore = displayScore`

주의:

- 1차 취향 수는 7개지만, 첫 방문 시 선택 개수는 최대 3개로 유지한다.
- FE/BE 모두 취향 추가가 쉬운 구조로 구현해야 한다.
- 취향 ID, 라벨, 설명, 매핑 점수축을 하드코딩 scattered state로 두지 말고 한 곳에서 관리해야 한다.

## personalizedReasons 계산

선택된 취향과 연결된 축 중 점수가 높은 최대 2개를 문자열로 만든다.

정확한 규칙:

1. 선택된 취향 순서를 먼저 유지
2. 각 취향의 원천 score field 값을 조회
3. 값이 0 이하인 항목은 이유에서 제외
4. 남은 항목 중 최대 2개만 사용
5. 문구 포맷은 `{이유 라벨} {소수점 한 자리}`

예:

- `조용함 4.8`
- `작업 적합성 4.6`

취향이 없거나 모든 값이 비어 있으면 빈 배열 반환

예:

- `조용함 4.8`
- `작업 적합성 4.6`

## isPersonalizedMatch 계산

1차 기준:

- `personalizedScore >= 4.0` 이면 `true`
- 아니면 `false`

## API 계약

## `GET /api/cafemap/stores`

추가 query:

- `preferenceIds=quiet_place,work_friendly`
- `mapMode=all|recommended_only`

query 처리 정책:

- 잘못된 취향 ID는 조용히 무시
- 중복 ID는 제거
- 최대 3개까지만 반영
- 유효한 ID가 0개면 취향 미선택과 동일하게 처리
- 이 경우 `400` 을 반환하지 않는다.
- `mapMode` 가 비어 있거나 잘못된 값이면 `all` 로 fallback

응답 추가 필드:

- `personalizedScore`
- `personalizedReasons`
- `isPersonalizedMatch`
- `seatComfortScore`
- `serviceScore`
- `atmosphereScore`
- `valueScore`

## `GET /api/cafemap/store-rankings`

이 엔드포인트는 1차에서 `취향` 탭 전용 리스트 API로 재사용한다.

추가 query:

- `preferenceIds=quiet_place,work_friendly`

query 처리 정책:

- 잘못된 취향 ID는 조용히 무시
- 중복 ID는 제거
- 최대 3개까지만 반영
- 유효한 ID가 0개면 기존 정렬 유지
- 이 경우 `400` 을 반환하지 않는다.

응답 추가 필드:

- `personalizedScore`
- `personalizedReasons`
- `isPersonalizedMatch`
- `seatComfortScore`
- `serviceScore`
- `atmosphereScore`
- `valueScore`

정렬 규칙:

- `preferenceIds`가 있으면 개인화 정렬
- 없으면 기존 정렬

## `GET /api/cafemap/rankings`

- 1차에서는 변경하지 않는다.
- 기존 전체 랭킹 탭에서 그대로 사용한다.

## Frontend 작업 단위

## FE-1. 취향 프리셋 모델 추가

파일:

- `front/lib/domain/entities/user_preference_preset.dart`

내용:

- `id`
- `label`
- `description`
- `icon`
- `scoreKeys`
- `reasonLabels`
- `matchMode`
- `enabled`

1차 프리셋:

- `quiet_place`
- `work_friendly`
- `good_atmosphere`
- `good_coffee`
- `good_value`
- `kind_service`
- `seat_comfortable`

구현 원칙:

- 프리셋 목록은 한 파일에서 선언하고, UI는 그 목록을 순회해서 렌더링해야 한다.
- 새 취향 추가 시 라우팅/위젯 조건문을 여러 군데 수정하지 않는 구조여야 한다.
- 1차는 단일 축만 쓰지만 모델은 복수 축 확장 가능 구조로 만든다.

필드 규칙:

- `scoreKeys`: 1차는 길이 1, 이후 복수 축 허용
- `reasonLabels`: `scoreKeys` 와 같은 순서의 라벨 목록
- `matchMode`: 1차는 모두 `average`, 이후 `weighted_average`, `inverse_preference` 등 확장 가능

권장 커밋:

- `feat(front): add preference preset model`

## FE-2. 취향 로컬 저장소 추가

파일:

- `front/lib/data/local/user_preference_storage.dart`

내용:

- `SharedPreferences` 기반 저장/로드
- 최대 3개 저장

권장 커밋:

- `feat(front): persist user preference selections`

## FE-3. 취향 provider 추가

파일:

- `front/lib/presentation/providers/user_preference_providers.dart`

내용:

- 선택 상태
- 로드/토글/완료/초기화 액션

권장 커밋:

- `feat(front): add user preference state`

## FE-4. 첫 방문 취향 시트 구현

대상:

- `front/lib/presentation/pages/preference_home_page.dart`

내용:

- 질문 시트
- 취향 칩
- `건너뛰기`
- `선택 완료`
- 저장값 없음 + 첫 진입일 때만 자동 오픈
- 재방문 수정 진입도 동일 시트 재사용

권장 커밋:

- `feat(front): add first-visit preference sheet`

## FE-5. 바텀 탭 개편

대상 파일:

- `front/lib/presentation/pages/main_shell.dart`
- `front/lib/app/router.dart`
- `front/lib/core/constants/app_strings.dart`

변경:

- `취향 / 랭킹 / 맵 / 내 리뷰`

권장 커밋:

- `refactor(front): reorganize bottom navigation`

## FE-6. 취향 탭 리스트 구현

신규 파일:

- `front/lib/presentation/pages/preference_home_page.dart`

대상 파일:

- `front/lib/presentation/widgets/store_ranking_card.dart`
- 필요 시 신규 카드 보조 위젯

내용:

- 취향 요약 바
- 수정/초기화
- 개인화 리스트
- 추천 이유 노출
- 빈 상태/로딩 상태/에러 상태 구현

권장 커밋:

- `feat(front): add personalized preference list page`

## FE-7. 취향 리스트 API 연결

대상 파일:

- `front/lib/data/remote/ranking_api.dart`
- `front/lib/data/repositories/remote_ranking_repository.dart`
- `front/lib/presentation/providers/ranking_providers.dart`

내용:

- `/store-rankings` 호출 시 `preferenceIds` 전달
- 응답의 개인화 필드 파싱
- 응답의 `seatComfortScore`, `serviceScore`, `atmosphereScore`, `valueScore` 파싱

권장 커밋:

- `feat(front): connect preference list to personalized store rankings`

## FE-8. 랭킹 탭 유지 정리

대상 파일:

- `front/lib/presentation/pages/ranking_home_page.dart`

내용:

- 기존 전체 랭킹 성격 유지
- 필요 시 상단 문구를 `전체 랭킹` 맥락으로 정리

권장 커밋:

- `refactor(front): keep ranking tab as global ranking`

## FE-9. 맵 API 연결

대상 파일:

- `front/lib/data/remote/store_api.dart`
- `front/lib/data/repositories/remote_store_repository.dart`
- `front/lib/presentation/providers/store_providers.dart`

내용:

- `/stores` 호출 시 `preferenceIds`, `mapMode` 전달
- 개인화 필드 파싱
- 응답의 `seatComfortScore`, `serviceScore`, `atmosphereScore`, `valueScore` 파싱

권장 커밋:

- `feat(front): connect map to personalized store api`

## FE-10. 맵 UI 개인화 반영

대상 파일:

- `front/lib/presentation/pages/map_home_page.dart`
- `front/lib/presentation/pages/map_home/map_home_widgets.dart`
- `front/lib/presentation/pages/map_home/map_home_marker_bundle.dart`

내용:

- 취향 요약 바
- `전체 보기 / 추천만 보기`
- 추천 마커 스타일
- 추천 이유 카드

권장 커밋:

- `feat(front): highlight personalized stores on map`

## FE-11. 내 리뷰 명칭 변경

대상 파일:

- `front/lib/presentation/pages/my_activity_page.dart`
- `front/lib/core/constants/app_strings.dart`

내용:

- `내 활동` -> `내 리뷰`

권장 커밋:

- `refactor(front): rename activity tab to my reviews`

## Backend 작업 단위

## BE-1. 개인화 응답 필드 추가

대상 파일:

- `back/cafemap/schemas/cafemap.py`

추가 필드:

- `personalizedScore: float = 0.0`
- `personalizedReasons: list[str] = Field(default_factory=list)`
- `isPersonalizedMatch: bool = False`
- `seatComfortScore: float = 0.0`
- `serviceScore: float = 0.0`
- `atmosphereScore: float = 0.0`
- `valueScore: float = 0.0`

주의:

- schema 필드 추가와 동시에 `router.py` 응답 매핑도 같이 수정해야 한다.
- 프론트는 camelCase 이름을 그대로 사용하므로 schema 이름을 source of truth로 삼는다.

권장 커밋:

- `feat(back): add personalized fields to store schemas`

## BE-2. 개인화 계산 서비스 추가

신규 파일:

- `back/cafemap/services/personalization_service.py`

필수 함수:

- `parse_preference_ids(raw: str | None) -> list[str]`
- `get_preference_specs() -> dict[str, PreferenceSpec]`
- `compute_personalized_score(store_like, ids: list[str]) -> float`
- `build_personalized_reasons(store_like, ids: list[str]) -> list[str]`
- `is_personalized_match(score: float) -> bool`

1차 지원 ID:

- `quiet_place`
- `work_friendly`
- `good_atmosphere`
- `good_coffee`
- `good_value`
- `kind_service`
- `seat_comfortable`

구현 원칙:

- 취향 ID -> 응답 score field 매핑은 한 곳에서 관리한다.
- 새 취향 추가 시 route 로직을 복붙하지 않고 매핑 테이블만 추가하면 되게 만든다.
- 1차는 단일 축이지만 `PreferenceSpec.score_keys: list[str]` 구조로 만든다.

PreferenceSpec 권장 구조:

```python
@dataclass(frozen=True)
class PreferenceSpec:
    id: str
    label: str
    score_keys: list[str]
    reason_labels: list[str]
    match_mode: str
```

권장 커밋:

- `feat(back): add personalization scoring service`

## BE-3. `/store-rankings` 개인화 반영

대상 파일:

- `back/cafemap/api/router.py`

내용:

- `preferenceIds` query 처리
- 개인화 점수 계산
- 개인화 이유 생성
- 취향이 있으면 개인화 정렬
- `seatComfortScore`, `serviceScore`, `atmosphereScore`, `valueScore` 응답 포함

권장 커밋:

- `feat(back): personalize store rankings for preference tab`

## BE-4. `/stores` 개인화 반영

대상 파일:

- `back/cafemap/api/router.py`

내용:

- `preferenceIds`
- `mapMode`
- `recommended_only` 모드 지원
- `seatComfortScore`, `serviceScore`, `atmosphereScore`, `valueScore` 응답 포함

권장 커밋:

- `feat(back): personalize store map results`

## BE-5. 랭킹 API 유지 확인

대상 파일:

- `back/cafemap/api/router.py`

내용:

- `/rankings`는 1차에서 그대로 유지
- 전체 랭킹 탭 동작에 영향 없도록 확인

권장 커밋:

- `chore(back): keep global ranking endpoint unchanged`

## 구현 순서

1. `BE-1`
2. `BE-2`
3. `BE-3`
4. `BE-4`
5. `BE-5`
6. `FE-1`
7. `FE-2`
8. `FE-3`
9. `FE-4`
10. `FE-5`
11. `FE-6`
12. `FE-7`
13. `FE-8`
14. `FE-9`
15. `FE-10`
16. `FE-11`

## 검증

프론트:

- `cd front && flutter analyze`

백엔드:

- `cd back && .\\venv\\Scripts\\python -m compileall cafemap`

## 이번 스코프에서 하지 않는 것

- 메뉴 랭킹 개인화
- 산미/바디/라떼 취향 지원
- 리뷰 기반 자동 취향 프로필
- 서버 저장형 취향 동기화
- 추천 알고리즘 고도화

## 확장성 요구사항

이번 구현은 1차 7개 취향을 지원하되, 이후 취향이 추가될 수 있다는 전제로 작성한다.

프론트 요구사항:

- 취향 목록은 선언형 데이터로 관리
- UI 렌더링은 목록 순회 기반
- 선택 상태는 취향 ID 배열 기반

백엔드 요구사항:

- 취향 ID와 점수축 매핑은 테이블/딕셔너리 형태로 관리
- 점수 계산과 추천 이유 생성은 공통 함수 사용
- 새 취향 추가 시 schema 전체를 갈아엎지 않고 매핑과 응답 필드만 확장 가능해야 함

## 2차 확장

2차에서는 아래를 추가한다.

- 세부 커피/공간 축 응답 확장
- `산미 적은 커피`, `바디감 있는 커피`, `라떼가 좋은 곳`, `디저트가 괜찮은 곳`, `응대가 친절한 곳`, `분위기가 좋은 곳`, `가성비가 좋은 곳` 지원
- 리뷰 기반 취향 프로필 API
