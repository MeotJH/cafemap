# 장소검색·리뷰작성 개선 계획

## 문제 정의

현재 지도의 `새로운 카페 찾기`는 MVP 수준으로 동작한다.

- 백엔드 `places/search`는 `display=20`을 받아도 네이버 지역 검색 API 특성상 한 번에 많은 결과를 주지 못한다.
- 현재 검색은 좌표 기반 반경 검색이 아니라 `"카페"` 키워드 기반이라, 사용자의 현재 위치 근처를 정확히 반영하지 못한다.
- 모바일 웹 리뷰 작성 화면은 키보드가 올라오면 하단 CTA와 입력 폼의 배치가 어색해지고, 일부 기기에서 제출 버튼이 화면을 막는 것처럼 보인다.

이번 작업의 목표는 세 가지다.

1. `places/search`를 좌표 중심의 주변 카페 탐색용으로 확장한다.
2. 프론트 지도 화면이 그 계약을 사용해 실제로 "근처의 새로운 카페"를 더 자연스럽게 보여주게 한다.
3. 리뷰 작성 화면을 모바일 웹 기준으로 다시 배치해 키보드와 충돌하지 않게 한다.

## 기획 판단

### 사용자 문제

- 지도에서 `새로운 카페 찾기`를 눌렀는데 결과가 적거나 멀리 있는 곳이 섞이면 신뢰가 떨어진다.
- 리뷰 작성 중 키보드가 올라왔을 때 CTA가 입력 흐름을 방해하면 제출 화면이 깨진 것으로 인식된다.

### 목표 사용자 경험

- 지도에서 `새로운 카페 찾기`를 누르면 현재 위치 기준 근처 카페가 여러 개 뜬다.
- 기존에 리뷰가 있는 카페는 그대로 유지하고, 새 카페만 별도로 탐색할 수 있다.
- 리뷰 작성 중에는 키보드가 올라와도 텍스트 입력과 제출 버튼 접근이 유지된다.

### 비목표

- 이번 단계에서 네이버 지도 수준의 완전한 반경 장소 API를 새로 붙이지는 않는다.
- 실제 장소 사진 썸네일을 외부에서 수집하는 작업은 포함하지 않는다.
- 신규 카페 결과를 영구 저장하거나 사전 캐싱하지 않는다.

## 디자이너 판단

### 지도 화면

- `새로운 카페 찾기`는 한 번 누르면 결과가 명확히 바뀌는 액션이어야 한다.
- 결과가 적게 뜨는 것보다 "근처 위주"라는 인상이 중요하다.
- 리뷰된 카페와 새 카페의 마커 구분은 유지하되, 결과 상태 문구를 함께 보여줘야 한다.

### 리뷰 작성 화면

- 모바일 웹에서 하단 고정 CTA는 키보드와 쉽게 충돌한다.
- 입력이 긴 화면에서는 버튼도 스크롤 컨텍스트 안에 있거나, 최소한 키보드 높이에 맞춰 자연스럽게 따라 올라와야 한다.
- 상단 카페 정보와 브랜드/메뉴 선택은 고정 블록처럼 보이되, 전체 화면은 하나의 스크롤 흐름으로 읽혀야 한다.

## 백엔드 설계

### 현재 한계

- 네이버 지역 검색 API 공식 문서상 `display`는 최댓값 5이고, `start`는 최대 1000까지 가능하다.
- 이 API는 좌표 반경 파라미터를 직접 제공하지 않는다.

출처:
- https://developers.naver.com/docs/serviceapi/search/local/local.md

### 해결 방향

`places/search`에 아래 optional 파라미터를 추가한다.

- `lat`
- `lng`
- `radiusKm`
- `pages`

동작 방식:

1. 기본 검색어는 여전히 `query`를 사용한다.
2. `pages` 만큼 `start` 값을 증가시키며 네이버 지역 검색을 여러 번 호출한다.
3. 중복 장소를 `placeId` 수준에서 제거한다.
4. `lat/lng`가 있으면 서버에서 거리 계산을 수행한다.
5. `radiusKm`가 있으면 반경 안 결과만 남긴다.
6. 정렬은 거리 오름차순 우선으로 통일한다.

즉, "진짜 반경 검색 API"를 새로 붙이는 대신, 현재 계약 안에서 가능한 범위의 좌표 필터링을 서버에서 책임지는 구조다.

### 계약 변경

요청:

- `GET /api/cafemap/places/search?query=카페&lat=37.5&lng=127.0&radiusKm=3&pages=6`

응답 필드 추가:

- `lat`
- `lng`
- `distanceKm`

`mapx/mapy`는 하위 호환을 위해 유지 가능하지만, 프론트는 새 필드 사용으로 전환한다.

## 프론트엔드 설계

### 지도

- `새로운 카페 찾기`는 현재 위치가 있을 때 `lat/lng/radiusKm/pages`를 함께 넘긴다.
- 프론트의 거리 계산/좌표 변환 책임은 최소화하고, 서버가 준 `lat/lng/distanceKm`를 우선 사용한다.
- 리뷰된 카페와 이름/좌표가 겹치는 결과는 프론트에서 마지막 한 번만 보조 필터링한다.

### 리뷰 작성

- 화면 전체를 하나의 스크롤 흐름으로 재구성한다.
- 하단 CTA는 `SafeArea + AnimatedPadding(viewInsets.bottom)`로 키보드 위에 자연스럽게 위치시킨다.
- 본문 하단에는 CTA 높이만큼 여유 패딩을 넣어 마지막 입력 필드가 버튼 뒤로 숨지 않게 한다.
- 포커스 해제용 배경 탭 처리도 같이 넣는다.

## 계약 동기화 체크포인트

- 백엔드 `PlaceSearchOut`와 프론트 `PlaceSearchResult` 필드 일치 여부
- `lat/lng/distanceKm` nullability
- 기존 `mapx/mapy`를 아직 쓰는 경로가 남아 있는지
- 지도 마커 ID 생성 방식 변경 시 회귀 여부

## 리뷰어 관점 체크

### 주요 리스크

- 페이지네이션으로 결과 수를 늘릴 때 중복 장소가 많이 섞일 수 있다.
- 거리 계산을 백엔드와 프론트가 동시에 하면 정렬 결과가 달라질 수 있다.
- 리뷰 작성 화면에서 CTA 배치를 바꾸면 기존 데스크톱 레이아웃이 어색해질 수 있다.

### 리뷰 포인트

- `places/search`가 위치 파라미터 없이도 기존처럼 동작하는지
- 위치 파라미터가 있을 때 거리순 정렬이 안정적인지
- 모바일 폭에서 키보드가 열린 상태로 리뷰 제출 버튼과 텍스트 영역 접근이 가능한지
- 지도와 매장 선택 화면이 새 응답 필드로 정상 렌더링되는지

## 구현 순서

1. 현재 지도 리팩토링 변경을 먼저 별도 커밋한다.
2. 백엔드 `places/search`에 페이지네이션 + 좌표 필터 + 거리 필드 추가
3. 프론트 `PlaceSearchResult` 계약 업데이트
4. 지도 화면을 새 계약 기반으로 단순화
5. 리뷰 작성 화면 레이아웃 수정
6. 프론트/백엔드 검증 후 후속 커밋

## 완료 기준

- `places/search`가 `lat/lng/radiusKm/pages`를 받아 여러 결과를 반환한다.
- 프론트 지도의 `새로운 카페 찾기` 결과가 현재 위치 기준으로 더 풍부하게 표시된다.
- 리뷰 작성 화면에서 모바일 웹 키보드 오픈 시 CTA가 입력 흐름을 방해하지 않는다.
- 프론트 `flutter analyze`와 백엔드 최소 실행 검증을 통과한다.
