# 리뷰 수정 업데이트 알고리즘

## 뷰 파일

### 파일
`front/lib/presentation/pages/review_detail_page.dart`

### 로직설명
- 리뷰 상세 화면에서 현재 로그인 사용자와 리뷰 작성자가 같은지 확인한다.
- 같으면 상단 우측에 `수정` 버튼을 노출한다.
- 버튼을 누르면 수정 라우트로 이동하면서 현재 리뷰 객체를 함께 넘긴다.

### 함수 및 로직설명
- `_canEditReview`
  - 로그인 사용자 이메일과 리뷰 작성자 이메일을 비교한다.
  - 같으면 수정 버튼 표시, 다르면 숨김 처리한다.
- 수정 버튼 `onPressed`
  - `/review/{id}/edit`로 이동한다.
  - `extra: review`로 현재 리뷰를 전달해 수정 폼 초기값으로 재사용한다.

## 뷰 파일

### 파일
`front/lib/presentation/pages/review_write_page.dart`

### 로직설명
- 작성 화면과 수정 화면을 같은 페이지로 재사용한다.
- `reviewId`가 있으면 수정 모드로 동작한다.
- 수정 모드에서는 기존 메뉴, 점수, 온도, 코멘트, 이미지 URL을 폼 상태로 복원한다.
- 저장 시 남겨둔 기존 이미지와 새로 업로드한 이미지를 합쳐 최종 `imageUrls`를 만든다.

### 함수 및 로직설명
- `_initializePage`
  - 수정 진입인데 `initialReview`가 없으면 서버에서 상세 리뷰를 다시 읽는다.
  - 읽은 리뷰로 폼 초기값을 구성한다.
- `_applyInitialReview`
  - 기존 리뷰의 점수, 총점, 온도, 코멘트, 기존 이미지 URL을 폼 상태로 옮긴다.
- `_loadMenus`
  - 브랜드 기준 메뉴 목록을 읽는다.
  - 기존 메뉴와 같으면 온도 선택도 복원한다.
  - 메뉴가 달라지면 온도는 다시 선택하게 비운다.
- `_submitReview`
  - 공통 유효성 검사를 수행한다.
  - 새 이미지가 있으면 presigned URL로 업로드한다.
  - `existingImageUrls + uploadedImageUrls`를 합쳐 최종 payload를 만든다.
  - 수정 모드면 `_updateReviewWithRecovery`, 작성 모드면 `_createReviewWithRecovery`를 호출한다.
- `_updateReviewWithRecovery`
  - `PUT /api/cafemap/reviews/{id}` 호출.
  - 인증 동기화가 필요한 401이면 auth sync 후 한 번 더 재시도한다.
- `_removeExistingImageAt`
  - 사용자가 기존 이미지를 지우면 `_existingImageUrls`에서 제거한다.
  - 저장 시 빠진 URL은 서버에 다시 보내지지 않으므로 결과적으로 삭제된다.
- `_invalidateReviewRelatedProviders`
  - 상세, 내 활동, 매장 리뷰, 랭킹, 매장 집계 provider 캐시를 비워서 수정 결과를 다시 읽게 만든다.

## 파일

### 파일
`front/lib/app/router.dart`

### 함수 및 로직설명
- `/review/:id/edit`
  - 수정 전용 라우트.
  - 로그인하지 않았으면 `/auth`로 리다이렉트한다.
  - `reviewId`와 `initialReview`를 `ReviewWritePage`에 전달한다.

### 파일
`front/lib/data/remote/review_api.dart`

### 함수 및 로직설명
- `updateReview`
  - 수정 API 호출 함수.
  - `PUT /api/cafemap/reviews/{id}`에 payload를 보낸다.
- `_reviewFromJson`
  - 수정 폼 복원에 필요한 `brandId`, `address`, `placeId`, `lat`, `lng`를 같이 파싱한다.

## 백엔드

### 컨트롤러

#### 파일
`back/cafemap/api/router.py`

#### 로직설명
- `PUT /reviews/{review_id}` 엔드포인트를 추가한다.
- 인증 사용자를 확인한 뒤 서비스 레이어에 수정 요청을 넘긴다.
- 저장 후 최신 리뷰 상세를 다시 읽어 프론트가 바로 재사용할 수 있는 응답으로 반환한다.

## 백엔드

### 파일
`back/cafemap/services/review_service.py`

### 로직설명
- 수정 가능 권한을 확인한다.
- 수정 전 리뷰가 어느 브랜드/메뉴/매장에 속했는지 저장한다.
- 새 payload를 정규화해 리뷰 레코드를 갱신한다.
- 수정 후 영향받은 집계들을 다시 계산한다.

### 함수 및 로직설명
- `update_review`
  - 리뷰 존재 여부 확인.
  - 작성자 본인인지 확인.
  - 수정 전 `brand_id`, `menu_id`, `store_id` 저장.
  - 정규화된 payload로 리뷰 본문 갱신.
  - 이전 집계와 현재 집계를 모두 재계산한다.
- `_prepare_review_payload`
  - 생성/수정 공통 저장 규칙.
  - 브랜드, 메뉴, 매장, 점수, 이미지, 온도, 총점을 저장 가능한 형태로 정리한다.
- `_resolve_store_for_payload`
  - 전달된 브랜드/가게명/주소/placeId 기준으로 기존 매장을 찾는다.
  - 없으면 새 매장을 만들고, 있으면 주소/링크/좌표를 보강한다.
- `_rebuild_brand_menu_aggregate`
  - 해당 `brand_id + menu_id` 조합의 리뷰를 전부 다시 모아 평균 점수와 하이라이트를 계산한다.
  - 리뷰가 0개면 집계를 삭제한다.
- `_rebuild_store_aggregate`
  - 해당 매장의 리뷰를 전부 다시 모아 매장 평균과 항목별 평균, `counts_json`을 계산한다.
  - 리뷰가 0개면 집계를 삭제한다.

## 백엔드

### 파일
`back/cafemap/schemas/cafemap.py`

### 로직설명
- 리뷰 응답에 `brandId`, `address`, `placeId`, `lat`, `lng`를 추가한다.
- 프론트 수정 화면이 리뷰 상세 응답만으로 기존 상태를 복원할 수 있게 한다.

### 파일
`back/cafemap/repositories/review_repository.py`

### 로직설명
- 상세 조회 시 `Store` 전체를 같이 가져오도록 바꾼다.
- 그래서 주소, 링크, placeId, 좌표를 한 번에 응답에 실어 보낼 수 있다.

## 전체 업데이트 순서

1. 상세 화면에서 작성자 본인에게만 수정 버튼이 보인다.
2. 수정 버튼을 누르면 수정 라우트로 진입한다.
3. 수정 폼이 기존 리뷰 데이터를 초기값으로 복원한다.
4. 사용자가 점수, 코멘트, 기존 이미지, 새 이미지를 수정한다.
5. 프론트가 최종 payload를 만들어 `PUT /reviews/{id}` 호출한다.
6. 백엔드가 리뷰 레코드를 갱신한다.
7. 백엔드가 영향받은 메뉴 집계와 매장 집계를 다시 계산한다.
8. 프론트가 관련 provider 캐시를 invalidate 한다.
9. 사용자는 수정된 상세 화면으로 돌아오고, 이후 목록/랭킹/매장 화면도 최신 데이터로 다시 그려진다.
