# 카페부부 리팩토링 단계별 실행 문서

## 목적

- `cafebubu_mobile_refactor_plan.md`의 방향을 한 번에 전부 밀어 넣지 않고, 현재 코드베이스에서 안전하게 나눠서 구현한다.
- 기존 서비스가 이미 운영 중인 상태를 고려해, 작은 단위로 프론트/백엔드/데이터 계약을 정리한다.
- 각 단계는 독립적으로 검증 가능해야 하며, 중간 단계에서도 앱이 깨지지 않아야 한다.

## 문제 정의

- 현재 모바일 하단 구조는 `카페랭킹 / 메뉴랭킹 / 지도 / 내활동` 기준이다.
- 리팩토링안은 `홈 / 랭킹 / 지도 / 내기록` 구조와 `부부픽 / 아내픽 / 남편픽 / 사용자픽` 중심의 랭킹 경험을 요구한다.
- 하지만 현재 구현은 단일 지점 랭킹과 일반 리뷰 집계 중심이며, 공식 평가자 구분과 저장 카페 구조가 없다.
- 따라서 단순 문구 변경으로 끝나지 않고, UI 구조 변경과 API 계약 변경을 분리해서 진행해야 한다.

## 범위 구분

### 1차 범위

- 브랜딩 반영: `카페맵 -> 카페부부`
- 하단 탭 구조 변경
- 홈 화면 추가
- 메뉴랭킹 탭 제거
- `내활동 -> 내기록` 명칭 및 최소 구조 변경
- 랭킹 화면에서 `부부픽 / 아내픽 / 남편픽` 탭 UI 골격 추가

### 2차 범위

- 부부/아내/남편 랭킹을 실제 데이터로 분리
- 홈 요약 API 추가
- 상세 페이지 점수 분리
- 추천 메뉴, 최근 방문, 대표 카드 연결

### 3차 범위

- 사용자픽
- 저장한 카페
- `/ig` 랜딩
- 유입 최적화 및 확장 기능

## 단계별 실행 순서

### Step 1. 현재 구조 정리와 라우트 전환 준비

목표:
- 현재 `'/ranking'`, `'/menu-ranking'`, `'/map'`, `'/activity'` 구조를 새 정보구조로 바꿀 준비를 한다.

작업:
- `front/lib/app/router.dart`의 라우트 구조를 분석한다.
- `front/lib/presentation/pages/main_shell.dart`의 하단 탭 매핑을 `홈 / 랭킹 / 지도 / 내기록` 기준으로 재설계한다.
- 기존 `menu-ranking` 진입점을 제거해도 상세 진입이나 기존 기능이 막히지 않는지 확인한다.

산출물:
- 새 라우트 초안
- 탭 이동 정책
- 제거 대상 경로 목록

검증:
- 기존 화면 진입 경로 목록 정리
- 제거 후 남는 진입 경로 확인

### Step 2. 프론트 1차 셸 리팩토링

목표:
- 하단 탭 구조를 먼저 바꿔 앱의 골격을 문서 방향에 맞춘다.

작업:
- `MainShell`의 탭을 `홈 / 랭킹 / 지도 / 내기록`으로 교체한다.
- `카페랭킹 -> 랭킹`, `내활동 -> 내기록` 문구를 반영한다.
- `menu-ranking` 탭을 제거한다.
- 필요하면 기존 `RankingHomeCafe`를 랭킹 탭의 임시 기본 화면으로 유지한다.

영향 파일:
- `front/lib/presentation/pages/main_shell.dart`
- `front/lib/app/router.dart`
- 문자열 상수 파일

검증:
- `front/`에서 `flutter analyze`
- 탭 이동 수동 확인

### Step 3. 홈 화면 추가

목표:
- 서비스 정체성을 보여주는 새 진입 화면을 추가한다.

작업:
- `front/lib/presentation/pages/home_page.dart` 신규 생성
- 브랜드 영역, 슬로건, 오늘의 부부픽 자리, 최근 카페/추천 메뉴 섹션 골격 추가
- 초기에는 실제 API가 없어도 임시 데이터 또는 기존 랭킹 데이터 일부 재사용 가능
- 앱 초기 진입 라우트를 `'/ranking'`에서 `'/` 또는 `'/home'` 형태로 전환할지 확정

주의:
- 첫 단계에서는 완전한 실데이터 연결보다 레이아웃과 정보 흐름 고정이 우선

검증:
- `flutter analyze`
- 홈에서 랭킹/지도/상세 CTA 이동 확인

### Step 4. 랭킹 화면 UI 구조 전환

목표:
- 현재 `로컬/프랜차이즈` 중심 랭킹을 `부부픽 / 아내픽 / 남편픽 / 사용자픽` 구조로 바꿀 준비를 한다.

작업:
- `RankingHomeCafe` 내부 세그먼트 구조를 새 탭 구조로 교체한다.
- 초기에는 탭별 데이터가 같더라도 UI 구조부터 맞춘다.
- 정렬/필터는 MVP 범위만 노출한다.
- 사용자픽은 비활성/빈 상태 처리 가능하도록 설계한다.

영향 파일:
- `front/lib/presentation/pages/ranking_home/ranking_home_cafe.dart`
- 관련 header/types/widgets

검증:
- `flutter analyze`
- 탭 전환, 빈 상태, 상세 이동 확인

### Step 5. 내기록 최소 구조 전환

목표:
- 기존 리뷰 목록 중심의 `내활동`을 `내기록` 최소 버전으로 바꾼다.

작업:
- 페이지 타이틀 및 탭 명칭 변경
- 최소 범위는 `내가 평가한 카페`, `로그아웃` 유지
- 저장 카페는 실제 API 전까지 placeholder 또는 숨김 처리

영향 파일:
- `front/lib/presentation/pages/my_activity_page.dart`
- 인증/리뷰 provider 일부

검증:
- 로그인/비로그인 상태 진입 확인
- `flutter analyze`

### Step 6. 백엔드 계약 설계

목표:
- 부부픽 구조를 실제 데이터로 지원하기 위한 API/모델 변경 범위를 확정한다.

작업:
- 현재 `Review`, `StoreAggregate`, `StoreRankingOut` 구조 분석
- `reviewerType` 필요 여부 확정
- 공식 평가자(`WIFE`, `HUSBAND`)와 일반 사용자(`USER`) 분리 전략 작성
- `GET /api/home`, `GET /api/rankings?type=...` 등의 응답 계약 정의

영향 파일 후보:
- `back/cafemap/models/entities.py`
- `back/cafemap/schemas/cafemap.py`
- `back/cafemap/api/router.py`
- `back/cafemap/services/*`
- `back/cafemap/repositories/*`

검증:
- 계약 문서 초안 작성
- 프론트 필요한 필드 목록 대조

### Step 7. 랭킹 API 실제 분리 구현

목표:
- 프론트 탭이 실제로 서로 다른 랭킹 데이터를 받을 수 있게 만든다.

작업:
- 부부픽 계산 로직 추가
- 아내픽/남편픽/사용자픽 쿼리 분리
- 동점 처리 규칙 반영
- 데이터 부족 시 사용자픽 빈 상태 대응

검증:
- 백엔드 대상 경로 단위 테스트 또는 수동 API 호출
- 프론트 연결 후 탭별 응답 비교

### Step 8. 상세 페이지 확장

목표:
- 카페 상세에서 문서의 핵심 가치인 점수 구분과 추천 정보를 보여준다.

작업:
- 부부점수, 아내점수, 남편점수, 사용자 평균 구조 추가
- 추천 태그, 한 줄 결론, 추천 메뉴 영역 추가
- 가능하면 비슷한 카페 영역은 후순위로 둔다

영향 파일:
- `front/lib/presentation/pages/store_detail_page.dart`
- 상세 API, breakdown API

검증:
- `flutter analyze`
- 상세 화면 수동 점검


검증:
- 백엔드 단위 검증
- 프론트 수동 검증


## 권장 구현 순서

1. Step 1
2. Step 2
3. Step 3
4. Step 4
5. Step 5
6. Step 6
7. Step 7
8. Step 8

## 단계별 완료 기준

### 프론트 1차 완료 기준

- 하단 탭이 `홈 / 랭킹 / 지도 / 내기록`으로 동작한다.
- 메뉴랭킹이 하단 탭에서 사라진다.
- 홈 화면이 추가된다.
- `flutter analyze`가 통과한다.

### 랭킹 데이터 전환 완료 기준

- 랭킹 화면에서 `부부픽 / 아내픽 / 남편픽 / 사용자픽`이 구분된다.
- 사용자픽 데이터가 없을 때도 UI가 깨지지 않는다.
- 카페 상세에서 분리 점수를 보여줄 수 있다.

### 내기록 확장 완료 기준

- 내 평가와 저장 카페를 구분해서 볼 수 있다.
- 로그아웃과 인증 흐름이 유지된다.

## 리스크

- 현재 데이터 모델에 공식 평가자 구분이 없으면 부부픽 실구현이 지연될 수 있다.
- 메뉴랭킹 제거 이후 기존 메뉴 관련 진입 플로우가 사라질 수 있다.
- 홈 화면을 실데이터로 채우려면 랭킹 API보다 별도 요약 API가 더 효율적일 가능성이 높다.
- 저장 카페는 프론트보다 백엔드 설계가 먼저 필요하다.

## 권장 진행 방식

- 1차 PR: 셸 구조 + 홈 추가 + 명칭 변경
- 2차 PR: 랭킹 UI 구조 변경
- 3차 PR: 백엔드 계약/랭킹 API
- 4차 PR: 상세/내기록 확장
- 5차 PR: 저장/유입/확장 기능

## 메모

- 이 문서는 구현 순서를 정리한 실행 문서다.
- 상세 기획 원문은 `cafebubu_mobile_refactor_plan.md`를 기준으로 유지한다.
